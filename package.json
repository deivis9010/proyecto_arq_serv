{
  "name": "proyecto-arq-serv",
  "version": "1.0.0",
  "description": "API de empleados con Jest",
  "main": "src/server.js",
  "dependencies": {
    "express": "^5.1.0",
    "y": "^0.3.2"
  },
  "devDependencies": {
    "jest": "^30.0.5",
    "nodemon": "^3.1.10",
    "supertest": "^7.1.4"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "jest": {
    "testEnvironment": "node",
    "collectCoverageFrom": [
      "src/**/*.js",
      "!src/test/**"
    ]
  }
}
